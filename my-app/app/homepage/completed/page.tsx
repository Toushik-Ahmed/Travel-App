import Completed from '@/components/components/Completed';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Completed />
    </div>
  );
};

export default page;
